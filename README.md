# Usage

```
git clone https://github.com/proxymurder/server.git

cd server/

mkdir -p ext/

cd ext/

git clone https://github.com/jbeder/yaml-cpp.gitgit

clone https://github.com/proxymurder/clib.git

```

```
make cleanup cbuild

make --directory src/build

make crun ARGS="[<optional>]"
```

Clone -
Clone -
