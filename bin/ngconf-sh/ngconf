#!/bin/sh
. /usr/local/bin/ngconf.d/ngconf.lib

if [[ "$1" == '--debug' ]]; then
    shift 1
    NGCONF_DEBUG=1
fi
if [[ $1 == 'create' ]]; then
    shift 1
    __print_server_conf "${@}"
    # if [[ $1 == 'proxy' ]]; then
    #     shift 1
    #     if [[ $1 == '--redirect' ]]; then
    #         shift 1;
    #     fi
    #     proxy=${1?Error}
    #     __print_server_conf ${@} proxy ${proxy}
    # fi
fi

